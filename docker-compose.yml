---
version: '3.8'

services:
  docusaurus:
    container_name: docusaurus
    image: docusaurus
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - .:/home/app
