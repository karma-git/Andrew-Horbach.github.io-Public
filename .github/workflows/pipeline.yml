name: ci 
on:
  push:
    branches:
      - master 

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: redkubes/docusaurus-deploy-action@master
        with:
          source-folder: ./
          git-user: karma-git
          git-password: ${{ secrets.DOC_GH_TOKEN }}
          deployment-branch: gh-pages
